version: '3.1'

services:
  db:
    image: postgres
    container_name: booking-db
    volumes:
      - ./containers/postgresql:/var/lib/postgresql
    environment:
      - POSTGRES_USER=booking
      - POSTGRES_PASSWORD=password
  
  adminer:
    image: adminer
    container_name: booking-adminer
    ports:
      - 5656:8080

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    image: booking-frontend
    container_name: booking-frontend
    environment:
      - REACT_APP_API_URL=apiServer
      - REACT_APP_SOCKET_URL=wsServer
    links:
      - apiserver
      - wsserver
    ports:
      - 3000:3000
  
  apiserver:
    build:
      context: apiServer
      dockerfile: Dockerfile
    image: booking-api
    container_name: booking-api
    environment:
      - WS_API_URL=wsServer
      - DB_USER=booking
      - DB_PASSWORD=password
      - DB_HOST=db
      - DB_DATABASE=booking
    links:
      - db
  
  wsserver:
    build:
      context: wsServer
      dockerfile: Dockerfile
    image: booking-ws
    container_name: booking-ws
